extends layout

block content
    h1= title
    form(name="new-user", action="/login", method="post")
        div.input
            span.label userID
            input(type="text", name="userID")
        div.input
            span.label password
            input(type="text", name="password")
        div.actions
            input(type="submit", value="login")
